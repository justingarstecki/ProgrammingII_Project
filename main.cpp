#include <iostream>
#include "UserInterface.h"

using namespace std;

int main() {
    // Create the user interface object
    UserInterface ui;

    // Display the menu (skeleton placeholder for now)
    ui.displayMenu();

    cout << "Advanced Command-Line Calculator initialized." << endl;
    return 0;
}
